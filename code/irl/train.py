"""Training entry point (skeleton).

The real CLI and training orchestrator will be added later in Sprint 0 (Steps 3–7). This stub exists so imports and
entry points are stable from day one.
"""

from __future__ import annotations

from typing import List, Optional


def main(argv: Optional[List[str]] = None) -> None:  # pragma: no cover - placeholder
    """Temporary no-op main used during bootstrapping."""
    print("irl.train: training CLI will be implemented in later Sprint 0 steps.")


if __name__ == "__main__":
    main()
